<!doctype html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
  <title>Employees</title>
  <meta charset="utf-8"/>
  <link rel="stylesheet" th:href="@{/css/style.css}">
</head>
<body>
  <h1>Employees</h1>
  <p><a th:href="@{/employees/new}">Add Employee</a> | <a th:href="@{/logout}">Logout</a></p>
  <table>
    <thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Department</th><th>Designation</th><th>Actions</th></tr></thead>
    <tbody>
      <tr th:each="e : ${employees}">
        <td th:text="${e.id}">1</td>
        <td><a th:href="@{/employees/{id}(id=${e.id})}" th:text="${e.name}">Name</a></td>
        <td th:text="${e.email}">email</td>
        <td th:text="${e.department != null ? e.department.name : '—'}">Dept</td>
        <td th:text="${e.designation}">Desg</td>
        <td>
          <a th:href="@{/employees/edit/{id}(id=${e.id})}">Edit</a>
          <a th:href="@{/employees/delete/{id}(id=${e.id})}" onclick="return confirm('Delete?')">Delete</a>
        </td>
      </tr>
    </tbody>
  </table>
</body>
</html>
